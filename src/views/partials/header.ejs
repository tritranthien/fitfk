<header class="fixed top-0 left-0 right-0 z-50 bg-white shadow p-4 flex items-center justify-between">
  <h1 class="text-lg font-semibold">My App</h1>

  <% if (user) { %>
    <div class="flex items-center gap-4">
      <span class="text-gray-700 text-sm">Xin chào, <%= user.name %></span>
      <a href="/oauth/logout" class="text-red-600 hover:underline text-sm">Đăng xuất</a>
    </div>
  <% } %>
</header>
